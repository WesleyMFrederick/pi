# Per-repo sandbox allow manifest
# Paths listed here get write access in Seatbelt sandbox
#
# Format:
#   - One absolute path per line
#   - # comments and blank lines ignored
#   - Directories → (allow file-write* (subpath "..."))
#   - Files → (allow file-write* (literal "..."))
#   - Glob patterns supported (e.g., /path/to/*/.claude)
#
# Note: Reads are already allowed by default (no global deny file-read*)

# Sibling plugin repo (skills, hooks, scripts shared across repos)
/Users/wesleyfrederick/Documents/ObsidianVault/0_SoftwareDevelopment/cc-workflows-plugin

# Learnings repo — deterministic learning pipelines and learning files
# (previously 0-documents/learnings-md, now consolidated into 0-learnings)
/Users/wesleyfrederick/Documents/ObsidianVault/0_SoftwareDevelopment/0-learnings/learnings-files

# Documents repo — deterministic learning pipelines and learning files
# (previously 0-documents/learnings-md, now consolidated into 0-learnings)
/Users/wesleyfrederick/Documents/ObsidianVault/0_SoftwareDevelopment/0-documents

# Consumer repo .claude/ dirs — glob allows syncHarness writes to all repos (Type 1: accepted broad scope)
/Users/wesleyfrederick/.pi
